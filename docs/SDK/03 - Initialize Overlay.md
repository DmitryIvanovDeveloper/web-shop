### Overview
Initialize the SDK overlay for in-game UI.

Preconditions:
- User is logged in
- Access to SDK settings

Scenario (USER ACTION => SYSTEM REACTIONS):
1. User selects engine (Unity/Unreal) and initializes =>
  - System applies overlay configuration and shows preview

Expected Final State:
- Overlay is initialized and ready for integration
