### Overview
Apply the created theme to the interface.

Preconditions:
- User is logged in
- Theme is configured

Scenario (USER ACTION => SYSTEM REACTIONS):
1. User confirms theme application =>
  - System applies theme across the UI

Expected Final State:
- Theme applied successfully
