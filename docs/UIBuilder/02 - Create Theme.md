### Overview
Create a base theme for the UI.

Preconditions:
- User is logged in

Scenario (USER ACTION => SYSTEM REACTIONS):
1. User selects primary color and font =>
  - System applies theme settings

Expected Final State:
- Theme created and ready to apply across UI
