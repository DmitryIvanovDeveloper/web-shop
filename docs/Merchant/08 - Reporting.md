### Overview
Generate a campaign report to evaluate results.

Preconditions:
- User is logged in
- Campaign is active or completed

Scenario (USER ACTION => SYSTEM REACTIONS):
1. User sets report parameters (period, metrics) =>
  - System builds the report
2. User downloads the report =>
  - File is provided in the chosen format

Expected Final State:
- Report generated and downloaded
