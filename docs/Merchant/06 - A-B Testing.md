### Overview
Set up an A/B test to improve campaign performance.

Preconditions:
- User is logged in
- Multiple content variants available

Scenario (USER ACTION => SYSTEM REACTIONS):
1. User defines test parameters (variants, split) =>
  - Experiment settings are saved
2. After the test, user reviews results =>
  - Winner is highlighted with a suggestion to deploy

Expected Final State:
- Experiment configured; results ready for rollout
