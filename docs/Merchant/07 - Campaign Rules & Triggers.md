### Overview
Define automated rules and triggers for campaigns.

Preconditions:
- User is logged in
- Campaign is in configuration

Scenario (USER ACTION => SYSTEM REACTIONS):
1. User specifies trigger and action =>
  - Rule is saved with priority

Expected Final State:
- Automation rules added to the campaign
