### Overview
Export campaign and user data for external analysis.

Preconditions:
- User is logged in
- Access to export tools

Scenario (USER ACTION => SYSTEM REACTIONS):
1. User chooses a dataset to export =>
  - System prepares a CSV snapshot
2. User runs the export =>
  - Downloadable file becomes available

Expected Final State:
- Data export completed; file downloaded by the user
